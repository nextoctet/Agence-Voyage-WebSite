import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'almasvoyages.com', 'upload.wikimedia.org', 'www.mapsofworld.com', 'media.meer.com'],
  },
};

export default nextConfig;